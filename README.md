# pesca

Configuration in `pesca.yml`

To run in the console with pretty-printed logs:

    deno task start

To run in background using nohup:

    nohup deno task main & tail -F nohup.out | deno task pretty